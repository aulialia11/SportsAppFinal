<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dicoding.sportsapp">

    <uses-permission android:name="android.permission.INTERNET" />


    <application
        android:name="com.dicoding.sportsapp.app.MyApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SportsApp">
        <activity
            android:name="com.dicoding.sportsapp.main.MainActivity"
            android:theme="@style/Theme.SportsApp.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="com.dicoding.sportsapp.detail.ActivityDetailSport"
            android:label="@string/title_activity_detail_home"
            android:theme="@style/Theme.SportsApp.NoActionBar" />


    </application>

</manifest>